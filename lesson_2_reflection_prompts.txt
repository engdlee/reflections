What happens when you initialize a repository? Why do you need to do it?
	
	A folder is created with the metadata of the status and history of the files, so git starts tracking the folder as a repository.

How is the staging area different from the working directory and the repository?
What value do you think it offers?
	
	I can select what files i can commit using the staging area, i can separate the changes i made logically.

How can you use the staging area to make sure you have one commit per logical
change?
	
	Staging the files that are part of the same logical change.

What are some situations when branches would be helpful in keeping your history
organized? How would branches help?
	
	Having different branches, like production and develop branches.

How do the diagrams help you visualize the branch structure?

	I can see a visual reference of how the branches are being constructed and how are they.

What is the result of merging two branches together? Why do we represent it in
the diagram the way we do?
	
	The changes of both branches are now in only 1 branch, because its the union of 2 branches.

What are the pros and cons of Git’s automatic merging vs. always doing merges
manually?

	Automtic Merge is good for changes that are not in the same line but it could add unwanted changes.
	Manual Merge is good for changes to the same lines but it could get tiresome if its a lot of changes.


